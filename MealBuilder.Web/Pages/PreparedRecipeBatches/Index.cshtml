@page
@model MealBuilder.Web.Pages.PreparedRecipeBatches.IndexModel

@{
    ViewData["Title"] = "Prepared Batches";
}

<h1>Prepared Batches</h1>

<p>
    <a asp-page="./Create" class="btn btn-primary">Create Prepared Batch</a>
</p>

<table class="table">
    <thead>
        <tr>
            <th>Recipe</th>
            <th>Cooked Date</th>
            <th>Total Servings</th>
            <th>Planned Days</th>
            <th>Servings Per Day</th>
            <th>Allocated Servings</th>
            <th>Unallocated Servings</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var summary in Model.PreparedRecipeBatchSummaries)
        {
            <tr>
                <td>@summary.PreparedRecipeBatch.RecipeNameSnapshot</td>
                <td>@summary.PreparedRecipeBatch.CookedDate</td>
                <td>@summary.PreparedRecipeBatch.TotalServings</td>
                <td>@summary.PreparedRecipeBatch.PlannedDays</td>
                <td>@summary.ServingsPerDay.ToString("0.00")</td>
                <td>@summary.AllocatedServings</td>
                <td>@summary.UnallocatedServings</td>
                <td>
                    <a asp-page="./Details" asp-route-id="@summary.PreparedRecipeBatch.Id">Details</a>
                    <a asp-page="./Delete" asp-route-id="@summary.PreparedRecipeBatch.Id">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>